# 历史静态前端（已弃用挂载）

本目录为早期单页 HTML 资源，**后端已不再挂载到 `/dashboard/`**。比赛与文本攻防演示请使用仓库根目录下的 **`web/`** Next.js 应用：

```bash
cd web && npm install && npm run dev
# 浏览器访问 http://localhost:3000
```

后端 API 文档：<http://127.0.0.1:8000/docs>
